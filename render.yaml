services:
  - type: web
    name: CRISS-AI
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    branch: main
    envVars:
      - key: SESSION_ID
        sync: "your-session-id-here"
      - key: OWNER_NUMBER
        sync: "your-owner-number-here"
