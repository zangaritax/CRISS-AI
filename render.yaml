services:
  - type: web
    name: criss-ai-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: SESSION_ID
        value: ""
      - key: AUTO_TYPING
        value: true
